DATABASE_DSN=postgresql://cpi:cpi@localhost:5432/cpi
AWS_ACCESS_KEY_ID=minioadmin
AWS_SECRET_ACCESS_KEY=minioadmin
AWS_ENDPOINT_URL=http://localhost:9000

# Optional. Daily pipeline commits only site/tracker/src/data/lab.json and pushes
# so Vercel rebuilds. Snapshot at /snapshot/lab.json updates live numbers without this.
# Fine-grained GitHub PAT, this repo, Contents: Read and write.
# GITHUB_TOKEN=

# Daily panel. sites1000 is the 1,000-site lab list. sites100k is 100,000 sites.
# CPI_PANEL=sites1000
