---
import Layout from "../../layouts/Layout.astro";
import DomainView from "../../components/DomainView.astro";

const domain = decodeURIComponent(Astro.params.domain || "");
---
<Layout title={`${domain || "Website"} — Crawl Policy Index`}>
  <DomainView domain={domain} rows={[]} />
</Layout>
